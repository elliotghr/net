namespace Genericos
{
    class ConHerencia
    {
        /*
        static void Main(string[] args)
        {
            AlmacenObjetos miAlmacen = new AlmacenObjetos(3);

            miAlmacen.agregar("Hola");
            miAlmacen.agregar("Mundo");
            miAlmacen.agregar("!");

            //Hacemos un casting de Object a string (string porque es el tipo de variable que tiene dentro)
            String elemento = (string)miAlmacen.getElemento(1);

            Console.WriteLine("El elemento es: " + elemento);

            // empleados

            miAlmacen.agregar(new Empleado(1500));
            miAlmacen.agregar(new Empleado(2500));
            miAlmacen.agregar(new Empleado(3500));

            //Hacemos un casting de Object a Empleado (Empleado porque es el tipo de variable que tiene dentro)
            Empleado elemento = (Empleado)miAlmacen.getElemento(1);

            Console.WriteLine("El elemento es: " + elemento.getSalario());

        }
        // clase para crear un array de cualquier tipo y que nos devuelva un objeto en cierta posición
        class AlmacenObjetos
        {
            // Creamos un constructor que pasa por parametro el # de elementos
            public AlmacenObjetos(int z)
            {
                datosElemento = new Object[z];
            }

            // método que permite agregar elementos
            public void agregar(Object obj)
            {
                //inserta
                datosElemento[i] = obj;
                // incrementamos el contador
                i++;
            }

            public Object getElemento(int posicion)
            {
                return datosElemento[posicion];
            }

            // Definimos un array y un contador
            private Object[] datosElemento;
            private int i = 0;
        }

        class Empleado
        {
            public Empleado(double salario)
            {
                this.salario = salario;
            }

            public double getSalario()
            {
                return this.salario;
            }

            private double salario;
        }
    */
    }
}
